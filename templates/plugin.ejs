import VueService from "vue-api-services";
import Vue from "vue";

<% const serialised = serialize(options || {}, { space: 2 }) %>

Vue.use(VueService, <%= serialised %>)

export default function(ctx, inject) {
    inject('serviceOptions', <%= serialised %>)
}
