import VueService from "vue-api-services";
import Vue from "vue";

Vue.use(VueService, <%= serialize(options || {}) %>)

export default function(ctx, inject) {
    inject('serviceOptions', <%= serialize(options || {}) %>)
}
